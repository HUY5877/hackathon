# 黑客松信息聚合平台 - 环境变量模板
# 复制此文件为 .env 并填入真实值
# cp .env.example .env

# ── 应用基础 ──────────────────────────────
APP_NAME=黑客松信息聚合平台
APP_VERSION=0.1.0
DEBUG=True

# ── 服务端口 ──────────────────────────────
HOST=0.0.0.0
PORT=8000

# ── 数据库 ────────────────────────────────
DATABASE_URL=postgresql+asyncpg://postgres:your_password@localhost:5432/hackathon
DATABASE_URL_SYNC=postgresql://postgres:your_password@localhost:5432/hackathon

# ── Redis（缓存 & Celery） ────────────────
REDIS_URL=redis://localhost:6379/0

# ── JWT 认证 ──────────────────────────────
# 生产环境务必替换为随机长字符串（>= 32 字符）
JWT_SECRET_KEY=change-me-to-a-secure-random-string
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ── LLM API（AI 数据清洗） ────────────────
# 阶跃星辰 Step API:
LLM_API_KEY=
LLM_API_BASE_URL=
LLM_MODEL=
LLM_SCREENING_API_BASE_URL=
LLM_SCREENING_MODEL=
LLM_SCREENING_WORKERS=2
LLM_SCREENING_BATCH_SIZE=100
LLM_SCREENING_SCAN_INTERVAL_SECONDS=300

# ── 邮件服务（EDM） ───────────────────────
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@example.com
SMTP_PASSWORD=

# ── 爬虫配置 ──────────────────────────────────────
CRAWLER_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/130.0.0.0 Safari/537.36
CRAWLER_REQUEST_DELAY=2.0
CRAWLER_PROXY_POOL=
CRAWLER_TIMEOUT=30.0
CRAWLER_MAX_RETRIES=3
CRAWLER_MAX_ITEMS_PER_PLATFORM=0
CRAWLER_UA_ROTATION=False
CRAWLER_LLM_CACHE_TTL=86400
CRAWLER_OUTPUT_DIR=crawl_output

# ── 跨域 ──────────────────────────────────
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]
